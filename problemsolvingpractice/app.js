/* const largest = [3, 5, 2, 8, 1];
largest.sort(function(a, b){return b - a});
console.log(largest[0])
 */

 /* const lettersWithStrings = ["#3", "$$$", "C%4!", "Hey!", "Hello", "Goodbye!"];
 const character = "!";
 function findExclamationPoint(lettersWithStrings, character){
     for(let i = 0; i <= lettersWithStrings.length; i++){
         let hasExclamationPoint = lettersWithStrings[i].includes(character)
         if(hasExclamationPoint === true){
             console.log(lettersWithStrings[i])
         }
     }
 }
 findExclamationPoint(lettersWithStrings, character) */



/* isDivisible(4, 2)
isDivisible(9, 3) 
isDivisible(7, 4)
isDivisible(15, 4) 
isDivisible(16, 4)
isDivisible(18, 4)

function isDivisible(num1, num2){
    if(num1 % num2 === 0){
        console.log("true")
    }else{
        console.log("false")
    }
} */